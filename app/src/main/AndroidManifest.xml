<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movid19">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditUpdateMovieActivity"></activity>
        <activity android:name=".EditMovieActivity" />
        <activity android:name=".AddMovieActivity" />
        <activity android:name=".EditUpdateUserActivity" />
        <activity android:name=".EditUserActivity" />
        <activity android:name=".AddUserActivity" />
        <activity android:name=".ManagerMainActivity" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".MainUserActivity" />
        <activity android:name=".PasswordActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>